{
  "schema_version": "commandgraph.command_card.v1",
  "command": "docker",
  "summary": "Build, run, inspect, and manage containers.",
  "aliases": ["container", "image", "docker compose"],
  "intents": ["list containers", "show docker images", "clean docker cache", "run container"],
  "default_risk": "medium",
  "risk_tags": ["container_runtime", "filesystem_write", "network_access"],
  "examples": [{"command": "docker ps", "explanation": "List running containers."}],
  "templates": [
    {"command": "docker ps", "description": "List running containers."},
    {"command": "docker images", "description": "List local images."}
  ]
}
